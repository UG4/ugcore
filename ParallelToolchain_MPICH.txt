################################################
# created by Sebastian Reiter
# s.b.reiter@googlemail.com
#
# Parallel Toolchain for OpenMPI
################################################

# set the compilers that are required by mpi
set(CMAKE_C_COMPILER mpicc)
set(CMAKE_CXX_COMPILER mpicxx)

# tell cmake to compile the parallel communication layer
set(COMPILE_WITH_PCL on)